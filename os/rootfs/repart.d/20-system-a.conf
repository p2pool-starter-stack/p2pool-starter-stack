# Slot A ships populated inside the image. Fixed size: an A/B pair must stay interchangeable, and
# a slot that grew on one machine could no longer receive a bundle built for the other.
[Partition]
Type=linux-generic
Label=system-a
SizeMinBytes=8G
SizeMaxBytes=8G
