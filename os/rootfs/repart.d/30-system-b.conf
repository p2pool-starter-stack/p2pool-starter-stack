# Slot B is created empty on first boot — RAUC populates it at the first update. Shipping it inside
# the image would add 8 GiB of zeros to every download.
[Partition]
Type=linux-generic
Label=system-b
SizeMinBytes=8G
SizeMaxBytes=8G
